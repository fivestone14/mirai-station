# Copy to .env (git-ignored) and fill in. Never commit .env, never paste the key into chat.
#   cp .env.example .env && chmod 600 .env
# The service and run-sndk-jev.sh read this file; a value already in the environment wins.
TYPESAFE_API_KEY=
